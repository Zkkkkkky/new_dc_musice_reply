# 项目记忆

本文件是本仓库的长期项目约定，供维护者和 Codex 在后续任务中共同遵守。若任务需要改变这些规则，应在同一次变更中更新本文件并说明原因。

## 项目目标

本仓库用于 NES ROM 音乐替换、扩容、探测和验证工作。所有修改都应可追溯、可复现，并明确区分输入、源码、中间产物、验证证据和最终交付物。

## 目录职责

| 路径 | 用途 | 是否提交 |
| --- | --- | --- |
| `src/` | 正式源码与核心实现，如汇编、Lua、Python 和配置源码 | 是 |
| `assets/` | 参与构建的版本化素材，如 NSF、音频、映射和图形资源 | 是 |
| `tools/` | 构建、探测、转换、校验等辅助工具 | 是 |
| `inputs/` | 用户提供的只读基线、原始 ROM 和外部输入 | 默认否，仅保留说明文件 |
| `build/` | 可由源码和输入重新生成的中间产物 | 否 |
| `dist/` | 已验证、可交付的最终产物 | 是 |
| `evidence/` | 为结论提供依据的日志、哈希、截图和测试结果 | 是，按任务精选 |
| `docs/` | 设计、研究、格式说明和操作文档 | 是 |
| `tests/` | 自动化测试、测试夹具和最小复现 | 是 |

## 强制规则

1. 仓库根目录只放入口文档、项目记忆、提交记录和通用配置；不得把源码、日志或新产物散落在根目录。
2. 可维护的实现放入 `src/`；仅用于开发或验证的脚本放入 `tools/`；参与构建且需要版本控制的音乐、音频和数据素材放入 `assets/`。脚本必须优先使用相对仓库根目录的路径，不得硬编码本机绝对路径。
3. `inputs/` 中的文件视为只读。任何转换都输出到 `build/`，不得原地覆盖输入。含版权、隐私或体积较大的原始文件不得提交，除非用户明确要求。需要长期版本控制的项目素材应整理到 `assets/`，而不是留在 `inputs/`。
4. `build/` 和临时文件必须可删除、可重建，禁止提交；代码和文档不得依赖某次本地构建残留。
5. 只有经过验证、准备交付的文件才能进入 `dist/`。ROM 放在 `dist/roms/`；替换或新增交付物时必须同步更新 `dist/MANIFEST.md` 的大小、SHA-256 和说明。
6. 验证日志和截图放在 `evidence/YYYY-MM-DD-主题/`。只保留能支撑结论的证据，避免提交无关的大型运行日志。
7. 设计说明、地址表、音乐映射、逆向结论和人工操作步骤放入 `docs/`，不得只存在于聊天记录中。
8. 禁止提交密钥、令牌、账号信息、模拟器个人配置、存档、缓存或本机环境文件。
9. 每次变更应运行与风险相称的验证；无法执行的验证要在交付说明和 `提交记录.md` 中明确注明。
10. 每个 Git 提交都应同步追加一条 `提交记录.md`，说明改动、实现方式、影响范围和验证结果。除非用户明确要求，不改写已有记录。

## 标准工作流

1. 把外部基线放入本地 `inputs/`，记录来源和哈希，保持原件不变。
2. 在 `src/` 实现正式修改，在 `assets/` 维护版本化素材，在 `tools/` 编写可复用的构建或探测工具。
3. 将生成过程的所有中间文件写入 `build/`。
4. 把验证结果归档到 `evidence/`，并把长期有效的结论整理进 `docs/`。
5. 验证通过后，将最终文件复制到 `dist/` 并更新交付物清单。

## 本地工具路径

- PC 端模拟器统一存放在 `D:\LLM\项目\fc模拟器`。后续需要查找、启动或配置 Mesen 等模拟器时，优先从此目录定位。

## 当前产物归类

- `dist/roms/新DC上.nes`：当前交付 ROM。
- `dist/roms/新DC下.nes`：当前交付 ROM。
- `dist/roms/新DC上_扩容15曲_未绑定.nes`：上版 1 MiB PRG、精修15曲未绑定交付 ROM。
- `dist/roms/新DC下_扩容15曲_未绑定.nes`：下版 1 MiB PRG、精修15曲未绑定交付 ROM。
- `assets/music/01 精修通用驱动（F000-F100-F160）/`：当前用于替换工作的版本化 NSF 音乐素材。
- `tools/build_refined15_roms.py` 与 `src/asm/`：两份 15 曲 ROM 的正式可重建实现。
