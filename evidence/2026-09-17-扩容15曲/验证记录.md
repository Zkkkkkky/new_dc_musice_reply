# 扩容 15 曲验证记录

日期：2026-09-17

## 产物

| ROM | 大小 | SHA-256 |
| --- | ---: | --- |
| 新DC上_扩容15曲_未绑定.nes | 1310736 | `6BB65EC535E31D7CC752E03F520AEB25E328D132E59CC69CC7C72C4D45528945` |
| 新DC下_扩容15曲_未绑定.nes | 1310736 | `F917FBAB44032A0E427228F7AA2002A7148CADCF995134445C7CD03AE4BC79CB` |

## 静态验证

- `python -m unittest discover -s tests -v`：4 项全部通过。
- 两份输出都只改 iNES 文件头偏移 `$04`；旧文件体 `$000010-$0C000F` 与各自源 ROM 逐字节相同。
- `$61-$63`、`$78-$7D` 均为空；15 曲、双引擎桥接、重定位驱动、56 音效和固定 Bank 副本均位于扩容区。
- 从当前仓库独立重建的两份输出，与旧工作区最后一版已验证未绑定 ROM 逐字节完全一致，差异均为 0 字节。

## Mesen 0.9.9

使用隔离临时目录、`--testrunner` 和 `src/lua/dc_refined15_mesen_smoke.lua` 分别运行两份 ROM。

| ROM | 退出码 | 结果 |
| --- | ---: | --- |
| 新DC上 | 0 | 通过 |
| 新DC下 | 0 | 通过 |

该矩阵覆盖 15 个命令、真实 PRG Bank 映射、驱动初始化/连续播放、APU 写入、旧 BSS 与零页越界、DMC 路径、原版 `$81` 交还、56 音效叠加以及 `$9D-$9F` 安全空命令。

## FCEUX 2.6.6

使用隐藏窗口的隔离进程和 `src/lua/fceux_refined15_track_smoke.lua` 分别运行两份 ROM。

| ROM | 结果 | 汇总 |
| --- | --- | --- |
| 新DC上 | 通过 | `pass=true tracks=15 failures=0` |
| 新DC下 | 通过 | `pass=true tracks=15 failures=0` |

逐曲日志：

- `FCEUX_新DC上_15曲矩阵.txt`
- `FCEUX_新DC下_15曲矩阵.txt`

两份日志均记录 15 首曲初始化、播放、返回、APU 写入和 `$5000=$27` 清锁命中。汇总计数一致：`init=238 play=3519 returns=3517 clears=3518 apu=25206`。

## 尚未执行

角色、地图、剧情事件的 15 曲最终绑定尚未执行，因为本轮没有收到绑定表。当前 ROM 为命令可触发的未绑定版，避免猜测并修改旧地址。
