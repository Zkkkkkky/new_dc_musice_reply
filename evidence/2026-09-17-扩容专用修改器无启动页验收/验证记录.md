# 扩容专用修改器无启动页验收

日期：2026-09-17

## 变更目标

保留已验证的扩容 CHR 读写核心，但用户从正式入口启动时不再看到显示贴吧、QQ 群的旧启动页，也无需手动点击“进入修改器”。

## 静态检查

- 外层入口：`dist/tools/新DC扩容专用修改器/新DC扩容专用修改器.exe`。
- 内部核心：`dist/tools/新DC扩容专用修改器/内部文件/修改器核心.exe`。
- 内部核心大小：5,701,632 字节。
- 内部核心 SHA-256：`4C7F2980CC780253050174C7A6E00A506C7D1EA29E74B90128BDA9ABD9335947`。
- 该核心与之前完成扩容 ROM 载入、三页签切换验收的 EXE 逐字节相同。
- 启动器只匹配精确标题 `SRW2修改器V1.5`，并在自动点击前保留三秒初始化时序。

## Windows 运行验收

1. 从正式根目录 EXE 启动。
2. 启动完成后，窗口枚举仅发现内部核心的 `SRW2扩容版修改器V1.0`，未发现 `SRW2修改器V1.5` 旧启动页。
3. 截图检查显示的是主界面，顶部菜单为“文件/帮助”，没有贴吧或 QQ 群内容。
4. 打开“文件→打开”成功显示 ROM 选择框，并正常列出 `dist/roms/` 中的 1,310,736 字节扩容 ROM。
5. 旧核心的完整 ROM 载入能力已由 `evidence/2026-09-17-扩容专用修改器验收-portable/` 验证；本次没有改动该核心的任何字节。

## 结论

启动页已从用户可见流程中移除。实现是外层入口隐藏并自动跳过旧页，而非继续修改未知的 VB 初始化代码；因此不会改变 ROM 地址、CHR 定位补丁或修改器主功能。
