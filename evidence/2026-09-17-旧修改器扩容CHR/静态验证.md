# 旧修改器扩容 CHR 补丁静态验证

## 输入与输出

- 输入：`inputs/legacy_modifier/SRW2_patched.exe`
- 输入大小：5,701,632 字节
- 输入 SHA-256：`FF1C1C5799FDB8F34DF355D761B382C6946CDADC6810772862947E892E8B4C27`
- 候选输出：`build/legacy_modifier/SRW2_expanded_chr_candidate.exe`
- 输出大小：5,701,632 字节
- 输出 SHA-256：`4C7F2980CC780253050174C7A6E00A506C7D1EA29E74B90128BDA9ABD9335947`

## 字节级验证

- 输入/输出长度一致。
- PE 头 `$000000-$000FFF` 逐字节一致。
- 总差异数：14 字节。
- 每处仅将小端 `0x080010` 的第三字节 `$08` 改为 `0x100010` 的 `$10`。
- 差异偏移：`$0BB2CB`、`$0BC751`、`$176EE4`、`$177202`、`$19F166`、`$1B61C9`、`$1BBA92`、`$1BD983`、`$1C34AC`、`$1C8F64`、`$1C9282`、`$1CFB15`、`$1F3846`、`$5108AE`。
- 输出中完整 `0x080010` 常量剩余 0 处；`0x100010` 常量数从 19 处增加到 33 处。

## 反汇编确认

13 处代码操作数均反汇编为完整有效指令：12 处 `mov dword ptr [ebp-...], 0x100010`，1 处 `push 0x100010`；另 1 处序列化数据为 `10 00 10 00`。

## 安全与运行状态

- 输入和输出 EXE 均未签名。
- Windows Defender 对候选输出执行自定义扫描后，未查到该路径的威胁命中记录。这不等价于信任或数字签名。
- 由于它是未签名且已修改的第三方 EXE，自动启动烟测被安全审批拒绝；未尝试绕过。
- 因此当前状态是 **静态验证通过、人工运行待验收**，不晋升到 `dist/`。

