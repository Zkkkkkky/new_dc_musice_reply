# LAST IMPRESSION 扩容 16 曲验证记录

日期：2026-09-19

## 输入与输出

- 输入 ROM SHA-256：`2D2DBB50B3240A972C79541542080FC293419970F2DA8D9E216D0221638E98DE`
- 输出 ROM：`dist/roms/新DC下_扩容16曲_LAST IMPRESSION_未绑定.nes`
- 输出大小：1,310,736 字节
- 输出 SHA-256：`EDFA40E25C7029E747CA72A25EFF100B035C55CB817AE2840E541229358A4984`
- 新曲命令：`$A6`
- 逻辑编号：`$23`
- 数据 Bank：`$78`

## 静态验证

- 目标 ROM 是 1 MiB PRG + 256 KiB CHR 的 Mapper 194 扩容布局。
- 原 15 曲 Bank `$65-$73` 逐字节保留。
- 新 Bank `$78` 与 NSF 的两个 4 KiB 曲目数据页逐字节一致。
- Bank `$79-$7D` 仍为全零保留区。
- Bank `$74/$75` 的重定位公共驱动主体保留，仅 `$BD00` 包装器增加 `$A6->$78` 映射。
- Bank `$60` 包含两处 `A9 27 8D 00 50`，恢复 FCEUX KT-008 高位锁存清理。
- 相对输入共改变 4,270 字节，全部位于审批的 Bank `$60/$64/$74/$75/$78` 槽位。

## FCEUX 2.6.6

`src/lua/fceux_refined16_track_smoke.lua` 的 16 曲命令矩阵通过：

`pass=true tracks=16 failures=0 init=239 play=3533 returns=3532 clears=3534 apu=25451`

新命令 `$A6` 有驱动初始化、连续播放、返回和 APU 写入记录。逐曲日志见 `FCEUX_新DC下_16曲矩阵.txt`。

## Mesen 边界

已准备聚焦 `$A6/$78` 的 `src/lua/dc_refined16_last_impression_mesen_smoke.lua`，但 Mesen/MesenCE 本机测试器以慢速实时帧运行；按用户要求不再继续等待长矩阵。本轮不声称 Mesen 全矩阵已通过。

## 绑定边界

新曲已可由命令 `$A6` 触发，但未修改角色、地图或剧情绑定表。
