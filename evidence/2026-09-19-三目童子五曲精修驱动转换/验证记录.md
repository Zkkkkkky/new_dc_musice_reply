# 三目童子五曲精修驱动转换验证

- 日期：2026-09-19
- 输入：用户提供的 5 个 `$8000/$B7B0/$8000` 三目童子 NSF（只读）
- 输出：5 个 `$F000/$F100/$F160` 精修公共驱动 NSF＋5 个 FamiStudio 文本工程
- 公共驱动 SHA-256：`20C0C421ADCE739DB24D8C9D445FB1FC6052C59668B3405967156833F402D431`

## 自动验证

- 五个原 NSF 除 `$B7B1=$05-$09` 外逐字节相同。
- 循环通过完整机器状态或长时间 APU 最终周期重复确定，没有用固定秒数硬截。
- 五个成品 NSF 的入口、Bank 表、驱动页、文件大小和 SHA-256 已锁定。
- 原曲对成品的可直接比较帧中，音高、音量、占空比、三角波和噪声误差均为 0。
- `Introduction` 原驱动的 216＋216 方波扫频帧已由 FamiStudio APU 导入器展开为实际听觉音高。
- 公共驱动重绑前后的逐帧听觉目标状态差异为 0。
- FamiStudio 4.5.3 对五个成品分别渲染 10.0 秒/44.1 kHz 单声道 WAV；五份均为 882,044 字节，RMS 为 3587.46—3898.07，峰值为 12072—18980，无静音或导出失败。试听 WAV 是可重建中间件，位于 `build/`，不提交。

详细源哈希、循环帧、Pattern 布局、成品哈希和逐帧统计见 `构建校验.json`。
