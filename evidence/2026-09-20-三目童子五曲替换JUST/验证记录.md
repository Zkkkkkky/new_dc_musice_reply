# 三目童子五曲替换 JUST COMMUNICATION 验证

- 日期：2026-09-20
- 上版输入 SHA-256：`490DC9C24829827FB52918E1FF53431B047B3C04DE77BDFBCD1745411FE4CFFF`
- 下版输入 SHA-256：`840E045A09A81CB8A9C58CA51FF9C0060F8E37826F25A58690EDAF598F1ED31C`
- 上版输出 SHA-256：`6D2B3435045FA646111155A29C2D206DA17694772961C85122036CAA37A0FF1D`
- 下版输出 SHA-256：`EF2CA2468B478BD93B4F405CE7D83BBAE030CB2610300EE795FECB24C7DB7DE5`

## 静态验证

- 两份输出均为 1,310,736 字节、1 MiB PRG＋256 KiB CHR、Mapper 194。
- `$6F/$79/$7A/$7C/$7D` 与五个 NSF 的数据页逐字节一致。
- Boss Fight 第三数据页位于 `$7B:$A000-$AFFF`，其重定位驱动核心与 `$74` 相同。
- 上版 `$78` 仍全零；下版 `$78` 与原 `LAST IMPRESSION` 数据逐字节一致。
- 原 PRG、两份 CHR、保留 Bank、现有精修曲、音效和固定 Bank 均通过允许修改范围门禁。

## Mesen 0.9.9

`dc_mitsume5_mesen_smoke.lua` 对上下版分别退出码 0，覆盖 `$A0/$A7/$A8/$A9/$AA`：

- 每首均命中重定位 INIT `$B100` 和连续 PLAY `$B160`；
- 数据 Bank 分别为 `$6F/$79/$7A/$7C/$7D`；
- Boss Fight 使用 `$7B` 驱动 Bank，其余使用 `$74`；
- APU 有活动、未进入 DMC 路径、没有写回旧 `$0200-$02A3` BSS；
- 最后成功交还原版音乐引擎。

下版另用 `dc_refined16_last_impression_mesen_smoke.lua` 复测 `$A6/$78`，退出码 0。

## FCEUX 2.6.6

- 上版 19 首精修曲矩阵：`pass=true tracks=19 failures=0`，INIT 242、PLAY 4558、返回 4560、APU 写入 34138。
- 下版 20 首精修曲矩阵：`pass=true tracks=20 failures=0`，INIT 243、PLAY 4797、返回 4800、`$5000=$27` 4800 次、APU 写入 36168。
- 五首新曲各自连续运行 239 次 PLAY。

逐曲日志见 `FCEUX_新DC上_19曲矩阵.txt` 与 `FCEUX_新DC下_20曲矩阵.txt`，机器可读构建报告见 `构建校验.json`。
