# 提交记录

## 2026-09-17 13:29:37 +08:00 — 初始化 NES ROM 资源

- 分支：`main`
- 基线提交：无（初始提交，仓库尚无 `HEAD`）
- 改了什么：
  - 纳入“新DC上”和“新DC下”两个 NES ROM 镜像，作为仓库的初始版本资源。
- 怎么改的：
  - 原样跟踪两个现有二进制镜像；两者均保留标准 `NES\x1A` 文件头及各自内容哈希。
- 影响范围：
  - `新DC上.nes`
  - `新DC下.nes`
- 验证：
  - 已核对两个文件大小均为 786448 字节，并检查其 16 字节 iNES 文件头。
  - SHA-256：`新DC上.nes` 为 `34A669879A1310045FBF15A916B86FBCDA765B3A1B39E799D5A76A16D03BB6EF`；`新DC下.nes` 为 `C623BBA0AE07AE3226A2AF09B1EAB1E42DB36EE3D8865D3DB20D3BF657C38CEC`。
  - 未运行模拟器或自动化测试。

## 2026-09-17 13:44:01 +08:00 — 规范项目目录与产物管理

- 分支：`main`
- 基线提交：`88b825e129c549f0c2ec1f3feaa382e5af3d60e4`
- 改了什么：
  - 建立源码、素材、工具、输入、中间产物、交付物、验证证据、文档和测试的目录规范，并将其写入项目级 `AGENTS.md`。
  - 将两个当前交付 ROM 迁移至 `dist/roms/`，新增包含大小和 SHA-256 的交付物清单。
  - 将 15 个精修 NSF 音乐文件归档为 `assets/music/` 下的版本化项目素材。
- 怎么改的：
  - 用根目录说明、各目录入口文档和 `.gitignore` 固化文件落位与版本控制规则。
  - 保持 ROM 二进制内容不变，仅调整路径；把可重建产物、本地输入、缓存、模拟器状态和未精选日志排除出版本控制。
- 影响范围：
  - `AGENTS.md`
  - `.gitignore`
  - `README.md`
  - `assets/`
  - `dist/`
  - `docs/`
  - `evidence/`
  - `inputs/`
  - `src/`
  - `tests/`
  - `tools/`
- 验证：
  - `git diff --check` 通过。
  - 两个迁移后 ROM 的 SHA-256 与迁移前一致，并与 `dist/MANIFEST.md` 相符。
  - 已确认 `assets/music/01 精修通用驱动（F000-F100-F160）/` 包含 15 个 NSF 文件。
  - 本次为目录和文档整理，未运行模拟器或自动化测试。

## 2026-09-17 13:59:13 +08:00 — 制定15首FamiStudio曲目扩容执行计划

- 分支：`main`
- 基线提交：`c2f4350ceed4ddfc0c4e058862cfb19774fe17c0`
- 改了什么：
  - 新增双ROM扩容、15首精修曲目集成、双引擎桥接、事件绑定、双模拟器验证和交付归档的分阶段执行计划。
  - 将原PRG `$00-$3F` 逐字节保护、扩容区优先、原空间修改例外审批和现有修改器兼容工作流设为明确约束。
  - 在文档索引中加入执行计划入口。
- 怎么改的：
  - 按阶段0至阶段8及G0至G8阶段门组织任务、产物和验收条件，并记录候选Bank布局、超长曲目处理方案与主要风险。
  - 规定默认采用“先用现有修改器修改原尺寸ROM，再运行扩容构建器”的顺序，避免CHR、固定Bank和音频副本失配。
- 影响范围：
  - `docs/15首FamiStudio曲目扩容改造执行计划.md`
  - `docs/README.md`
- 验证：
  - `git diff --check` 通过。
  - 本次仅修改文档，未运行代码测试或模拟器测试。

## 2026-09-17 15:36:23 +08:00 — 完成双ROM扩容与15首精修曲桥接

- 分支：`main`
- 基线提交：`5e5dd09df25f69686b83df2532ba56782ce50a9e`
- 改了什么：
  - 分别为“新DC上”和“新DC下”生成 Mapper 194、1 MiB PRG、256 KiB CHR 的15曲未绑定交付 ROM。
  - 纳入双引擎桥接、FCEUX KT-008 清锁交还、重定位后的精修曲公共驱动、15首歌曲数据、56个原版音效叠加数据和双模拟器回归脚本。
  - 删除旧测试版 `$9D-$9F` 三首曲的运行依赖；三个命令改为安全空命令。
  - 新增可重复构建器、自动化测试、命令表、布局说明、验证日志和交付清单。
- 怎么改的：
  - 采用与 `DC_kuorong.nes` 一致的“原文件体作为前缀＋CHR影子＋活动CHR后移”扩容布局。
  - 原 PRG `$00-$3F` 和旧文件体 `$000010-$0C000F` 整体逐字节不变；新增内容只放在 `$60-$7F`。
  - `$65-$73` 存放15首数据，`$74/$75` 存放重定位驱动，`$76` 为分发跳板，`$77` 为原版音效数据，`$7E/$7F` 为固定 Bank 副本。
  - 角色、地图和剧情绑定未作猜测；输出为命令可触发的“未绑定版”。
- 影响范围：
  - `tools/build_refined15_roms.py`
  - `src/asm/`
  - `src/lua/`
  - `assets/music/refined15_manifest.json`
  - `tests/test_refined15_build.py`
  - `docs/扩容15曲实现与命令表.md`
  - `evidence/2026-09-17-扩容15曲/`
  - `dist/roms/新DC上_扩容15曲_未绑定.nes`
  - `dist/roms/新DC下_扩容15曲_未绑定.nes`
  - `dist/roms/新DC上_扩容15曲_未绑定 - 副本.nes`
  - `dist/MANIFEST.md`
  - `.codex-remote-attachments/`
- 验证：
  - `python -m unittest discover -s tests -v`：4项通过。
  - Mesen 0.9.9：两份 ROM 的15曲完整矩阵均以退出码0通过。
  - FCEUX 2.6.6：两份 ROM 均为 `pass=true tracks=15 failures=0`。
  - 新构建结果与旧工作区最后一版已验证 ROM 逐字节一致，差异均为0字节。
  - 上版输出 SHA-256：`6BB65EC535E31D7CC752E03F520AEB25E328D132E59CC69CC7C72C4D45528945`。
  - 下版输出 SHA-256：`F917FBAB44032A0E427228F7AA2002A7148CADCF995134445C7CD03AE4BC79CB`。
  - 用户实验副本经静态比较确认只改动文件偏移 `$D052`：`$86→$9A`，SHA-256 为 `C064C18A08242403BE49F0F1583DC165EFFF5444AA48C7D5AF23C64832E5D31B`；该副本未运行模拟器验收。

## 2026-09-17 16:40:00 +08:00 — 新增扩容ROM标题编辑器

- 分支：`main`
- 基线提交：`91179e0561d36c14df47303ebae8147e375f7139`
- 改了什么：
  - 新增 Windows 可视化标题 CHR 编辑器，支持 512 KiB PRG 原版与 1 MiB PRG 扩容版 Mapper 194 ROM。
  - 可检测扩容 ROM 的旧 CHR 影子/活动 CHR 不一致，读取并修复 CT2 留在旧偏移的标题修改。
  - 提供四色 2bpp 像素绘制、撤销/重做、取色、128×112 PNG 导入导出和安全另存。
  - 新增便携源码目录、ZIP，以及不需要目标电脑安装 Python 的 Windows x64 单文件 EXE。
- 怎么改的：
  - 从 iNES Header 动态计算活动 CHR，标题逻辑偏移固定为 CHR `+$14000`，不依赖 CT2 的旧绝对偏移。
  - 扩容 ROM 另存时同时写入 `$094010-$094E0F` 影子与 `$114010-$114E0F` 活动标题，不允许覆盖源 ROM。
  - 源码版只依赖 Python 3.10+ 标准库和 Tkinter；EXE 用 PyInstaller 6.22.3 封装 Python 3.14.7 与 Tkinter，目标电脑不需要 Python。
- 影响范围：
  - `src/title_editor/`
  - `tools/启动扩容标题编辑器.cmd`
  - `tools/package_expanded_title_editor.py`
  - `tools/build_expanded_title_editor_exe.py`
  - `tools/title_editor_packaging_requirements.txt`
  - `tests/test_expanded_title_editor.py`
  - `docs/扩容ROM标题编辑器使用说明.md`
  - `dist/tools/扩容ROM标题编辑器/`
  - `dist/tools/扩容ROM标题编辑器.zip`
  - `dist/tools/扩容ROM标题编辑器.exe`
- 验证：
  - `python -m unittest discover -s tests -v`：11 项通过。
  - GUI 无头初始化通过，生成 14336 个可编辑像素单元。
  - 用用户 `2 (1).nes` 进行真实恢复演练：检出 2621 字节 CT2 影子修改，输出只将这 2621 字节同步到活动标题，标题区外改动为 0。
  - 恢复演练输出 SHA-256：`47251A6A6E851DDB5A28AE80BF0110D4424BE889B6065225BE644CD72E5935CD`；仅位于忽略的 `build/`，未作为 ROM 交付物。
  - 免 Python EXE 自检完整通过：独立启动 Tkinter，读取用户 `2 (1).nes`，同步 2621 字节 CT2 标题修改，标题区外改动为 0，退出码 0。
  - EXE 连续两次构建哈希一致，Windows Defender 自定义扫描未发现威胁；SHA-256：`76AD83F909BB7F8BA5E12866BDBC844DB425B87C7BC7DE900EA95B6112E1565E`。
  - 便携源码 ZIP 采用固定时间戳可重复打包；SHA-256：`DB58988F6DA686AA5992DEC23BA93A393769F4B81CD835B3E195C66AB5D4B953`。

## 2026-09-17 16:52:00 +08:00 — 生成旧修改器扩容CHR兼容候选

- 分支：`main`
- 基线提交：`91179e0561d36c14df47303ebae8147e375f7139`
- 改了什么：
  - 审计 `inputs/legacy_modifier/SRW2_patched.exe`，确认其有 14 处将 CHR 起点硬编码为 `$080010`。
  - 新增哈希锁定的补丁器，将 13 处代码操作数和 1 处初始化数据改为扩容活动 CHR `$100010`。
  - 新增候选启动器、补丁说明、自动化测试与静态验证证据。
- 怎么改的：
  - 以输入 SHA-256 `FF1C1C5799FDB8F34DF355D761B382C6946CDADC6810772862947E892E8B4C27` 为强制版本门禁，不对未知版本套用固定偏移。
  - 输出到忽略的 `build/legacy_modifier/`，不覆盖 `inputs/` 原件，并生成 `.patch.json` 字节级审计记录。
  - 修改器保存扩容 ROM 的历史备份显示文件始终保持 1,310,736 字节，不会因普通 PRG 修改而截断扩容区。
- 影响范围：
  - `tools/patch_legacy_modifier_expanded_chr.py`
  - `tools/launch_legacy_modifier_expanded_chr_candidate.cmd`
  - `tests/test_legacy_modifier_expanded_chr.py`
  - `docs/旧修改器扩容CHR兼容补丁.md`
  - `evidence/2026-09-17-旧修改器扩容CHR/`
- 验证：
  - `python -m unittest discover -s tests -v`：13 项通过。
  - 候选 EXE 与输入等长，PE 头不变，仅 14 个审计字节不同；反汇编后 13 处代码均为完整的 `$100010` 操作数。
  - 候选 EXE SHA-256：`4C7F2980CC780253050174C7A6E00A506C7D1EA29E74B90128BDA9ABD9335947`。
  - Windows Defender 自定义扫描未产生该候选路径的威胁命中记录，但输入/输出均未签名。
  - 自动启动烟测因未签名第三方 EXE 的安全风险被审批拒绝，未尝试绕过；当前为静态验证通过、用户运行待验收候选，尚未进入 `dist/`。

## 2026-09-17 17:05:00 +08:00 — 新增扩容ROM全CHR同步工具

- 分支：`main`
- 基线提交：`91179e0561d36c14df47303ebae8147e375f7139`
- 改了什么：
  - 新增独立 Windows GUI，将原版旧偏移修改器写入的完整 256 KiB CHR 影子同步到扩容 ROM 活动 CHR。
  - 打开后显示两份 CHR 的差异字节数和首末范围，同步前再次确认方向。
  - 交付便携目录和可重复打包的 ZIP。
  - 新增不依赖 Python 的 .NET Framework C# WinForms 单文件 EXE。
- 怎么改的：
  - 严格验证 Mapper 194、1 MiB PRG、256 KiB CHR、无 Trainer 和 `0x140010` 文件长度。
  - 将 `0x080010-0x0C000F` 作为权威源，复制到 `0x100010-0x14000F`；边界门禁保证其他任何字节不变。
  - 禁止覆盖源 ROM，使用同目录临时文件和原子替换生成新 ROM，并在保存后重新校验两份 CHR 完全一致。
  - 文档明确禁止在“直接写活动 CHR”的已补丁修改器之后运行本工具。
- 影响范围：
  - `src/chr_sync/`
  - `tools/启动扩容ROM全CHR同步工具.cmd`
  - `tools/package_expanded_chr_sync.py`
  - `tools/build_expanded_chr_sync_exe.py`
  - `tests/test_expanded_chr_sync.py`
  - `docs/扩容ROM全CHR同步工具使用说明.md`
  - `evidence/2026-09-17-全CHR同步/`
  - `dist/tools/扩容ROM全CHR同步工具/`
  - `dist/tools/扩容ROM全CHR同步工具.zip`
  - `dist/tools/扩容ROM全CHR同步工具.exe`
- 验证：
  - `python -m unittest discover -s tests -v`：16 项通过。
  - GUI 无头初始化通过。
  - 用户 `2 (1).nes` 实测检出 2,621 字节差异；输出只在活动 CHR 写入 2,621 字节，其他区域变化为 0，同步后两副本差异为 0。
  - 真实演练输出 SHA-256：`47251A6A6E851DDB5A28AE80BF0110D4424BE889B6065225BE644CD72E5935CD`；只位于忽略的 `build/`。
  - 便携 ZIP 连续两次构建哈希一致，大小 9,785 字节，SHA-256：`A34BBDD4BCDCC0D1B27D7CF60D9F0A53E74CC462E2E90A3E991EE3E56BBD3E9F`。
  - 最终免 Python EXE 用用户 `2 (1).nes` 自检通过，活动 CHR 外改动为 0；体积 12,800 字节，SHA-256：`7BAE2B95E18F7D6165F49F072F937103CB825828C5C5DEDDF307D019B4D39851`。
  - Windows Defender 自定义扫描未发现该 EXE 的匹配威胁记录。
  - .NET Framework 旧 C# 编译器会改变每次构建的模块标识，EXE 未达成逐字节可重复构建；交付清单锁定最终哈希。
  - 尚未在 FCEUX/Mesen 人工观察同步后的画面。

## 2026-09-17 17:58:00 +08:00 — 确认刚达机体拼图在 FCEUX 生效

- 分支：`main`
- 基线提交：`91179e0561d36c14df47303ebae8147e375f7139`
- 改了什么：
  - 对用户的 `新DC (2)_CHR已同步.nes` 和 `新DC (3)_CHR已同步.nes` 做机体资源级差分。
  - 确认修改对象为机体 `$09` 「刚达」、主体拼图选择器 `$21`、CHR 图库页 `$37`。
  - 将已验证生效的用户 ROM 作为明确交付物放入 `dist/roms/`。
- 怎么改的：
  - 旧修改器写入 PRG 主体指针/脚本和旧 CHR 影子；全 CHR 同步工具将 `$37` 页的 683 个差异字节复制到活动 CHR。
  - FCEUX 自动战斗路线临时将已知战斗槽指向 `$09`，仅改 RAM，不改 ROM。
  - 记录资源分发器对 `$74/$09` 战斗外观配置和 `$21/$09` 主体拼图的真实请求。
- 影响范围：
  - `dist/roms/新DC_刚达机体拼图已生效_验证版.nes`
  - `dist/MANIFEST.md`
  - `docs/扩容ROM全CHR同步工具使用说明.md`
  - `evidence/2026-09-17-机体拼图实机验证/`
- 验证：
  - FCEUX 2.6.6 修改前对照：战斗时请求 `$21/$09`，主体为空。
  - FCEUX 2.6.6 修改后：战斗时请求 `$21/$09`，新刚达主体已显示。
  - 交付 ROM 大小 1,310,736 字节，SHA-256：`7FC4889991BAE2F99308AECFC12A98F10413F6E0F675D0415D4B95607DEF8015`。

## 2026-09-17 19:08:31 +08:00 — 正式交付新DC扩容专用修改器

- 分支：`main`
- 基线提交：`91179e0561d36c14df47303ebae8147e375f7139`
- 改了什么：
  - 将此前静态验证的旧修改器扩容 CHR 补丁版整理为唯一 EXE 便携交付物。
  - 配套必要 INI/DAT 默认配置，不携带旧测试 ROM、CDL 或调试文件。
  - 增加面向用户的使用说明，明确本版保存后禁止再执行旧 CHR 影子→活动 CHR 同步。
- 怎么改的：
  - 保持原 EXE 大小不变，仅将 13 处代码操作数和 1 处初始化数据的 CHR 基址从 `$080010` 改为 `$100010`。
  - 便携目录中仅保留 `新DC扩容专用修改器.exe` 一个可执行文件，避免与标题编辑器、CHR 同步工具混淆。
  - 运行验收使用只读方式载入已验证的 1 MiB PRG 扩容 ROM，不覆盖输入或正式 ROM。
- 影响范围：
  - `dist/tools/新DC扩容专用修改器/`
  - `dist/tools/新DC扩容专用修改器.zip`
  - `dist/MANIFEST.md`
  - `docs/旧修改器扩容CHR兼容补丁.md`
  - `evidence/2026-09-17-扩容专用修改器验收-portable/`
- 验证：
  - EXE 启动存活检查：进程响应正常。
  - 便携目录实际进入 `SRW2扩容版修改器V1.0`，成功载入 1,310,736 字节扩容 ROM。
  - 三个主界面 `CPageControl` 页签均可切换；自动化验收退出码 0，目标进程已关闭。
  - 补丁范围单元测试 2 项通过；EXE SHA-256：`4C7F2980CC780253050174C7A6E00A506C7D1EA29E74B90128BDA9ABD9335947`。
  - 便携 ZIP 大小 1,534,462 字节，SHA-256：`BF4B0F0C94E1DAB07A501EDEE6BBA5C8F51790CA121B0C8B3B6477040864B5DB`。

## 2026-09-17 19:13:31 +08:00 — 交付扩容ROM图形修改工具与实机验证

- 分支：`main`
- 基线提交：`91179e0561d36c14df47303ebae8147e375f7139`
- 改了什么：
  - 交付扩容 ROM 标题编辑器、完整 CHR 同步工具和直接读写活动 CHR 的新DC扩容专用旧修改器。
  - 纳入刚达 `$09` 机体拼图已生效 ROM，以及标题、机体拼图和修改器运行验收证据。
  - 补齐三类工具的源码、构建打包脚本、自动化测试、地址说明、使用流程和交付清单。
- 怎么改的：
  - 标题编辑器和同步工具根据 iNES Header 区分旧 CHR 影子 `$080010` 与活动 CHR `$100010`，保存时执行严格边界检查。
  - 对已审计旧修改器的 14 处 CHR 基址常量执行哈希锁定补丁，并将必要 INI/DAT 配置打包为不含测试 ROM 的便携版。
  - 通过 FCEUX 运行时资源请求和战斗截图确认刚达主体拼图生效，并通过 Windows GUI 自动化确认专用修改器可载入扩容 ROM。
- 影响范围：
  - `src/title_editor/`、`src/chr_sync/`
  - `tools/`、`tests/`
  - `dist/tools/`、`dist/roms/新DC_刚达机体拼图已生效_验证版.nes`
  - `docs/`、`evidence/`、`dist/MANIFEST.md`
- 验证：
  - `python -m unittest discover -s tests -v`：16 项通过。
  - 新DC扩容专用修改器便携版成功进入主界面、载入 1,310,736 字节扩容 ROM并切换三个页签，验收退出码 0。
  - FCEUX 2.6.6 修改前后对照确认刚达 `$09` 主体拼图由空白变为新机体图形。
  - ZIP 内容检查确认扩容专用修改器包不含 `.nes`、`.cdl` 或 `.deb`；交付物大小和 SHA-256 已写入 `dist/MANIFEST.md`。

## 2026-09-17 20:45:37 +08:00 — 修复扩容修改器CHR保存并跳过启动页

- 分支：`main`
- 基线提交：`7b24faf1164fc1994bb33fb9300e7a05c6e3d898`
- 改了什么：
  - 新增可审计的 C# 外层入口，隐藏并自动跳过显示贴吧、QQ 群的旧启动页；用户只需运行根目录的唯一 EXE。
  - 根据用户提供的琉妮机体修改前后 ROM，确认旧核心遗漏的保存路径只改动旧 CHR 影子，活动 CHR 完全没有变化。
  - 扩容专用入口在整个修改器会话中保持运行，监控当前 ROM 保存并自动协调旧影子与活动 CHR 的单边写入。
  - 更新便携交付包、使用说明、清单、真实样本证据和自动化测试。
- 怎么改的：
  - 入口精确匹配 `SRW2修改器V1.5`，将其隐藏，等待三秒初始化后自动进入 `SRW2扩容版修改器V1.0`；启动前校验内部核心 SHA-256。
  - 打开 ROM 时分别保存旧影子 `$080010-$0C000F` 与活动 CHR `$100010-$14000F` 快照。
  - 保存后逐字节判断变化方向；只在单侧变化时复制到另一侧，两侧同时出现不同新值则拒绝覆盖并报警。
  - 使用同目录临时文件与原子替换更新 ROM，并严格拒绝非 Mapper 194、非1 MiB PRG/256 KiB CHR 或含 Trainer 的文件。
- 影响范围：
  - `src/modifier_launcher/`
  - `tools/build_modifier_skip_launcher.py`
  - `tests/test_modifier_skip_launcher.py`
  - `dist/tools/新DC扩容专用修改器/`
  - `dist/tools/新DC扩容专用修改器.zip`
  - `docs/旧修改器扩容CHR兼容补丁.md`
  - `evidence/2026-09-17-修改器CHR自动同步/`
  - `evidence/2026-09-17-扩容专用修改器无启动页验收/`
  - `dist/MANIFEST.md`
- 验证：
  - Windows 实际启动后仅显示 `SRW2扩容版修改器V1.0`，旧启动页不可见；“文件→打开”可正常显示扩容 ROM。
  - 用户样本中检出旧影子236字节变化、活动 CHR 0字节变化；原始扩容音乐区0字节变化。
  - 修复后的测试输出仅在活动 CHR 对应位置补写236字节，同步后两份 CHR 完全一致，SHA-256 为 `339955BDFC1655A04D611865A2C02ECD0F73B519130BDF8F53055FE66B7C673C`。
  - 修改器专项测试7项全部通过；全测试发现20项，其中19项通过，1项因环境缺少 `capstone` 无法导入。
  - 最终入口 SHA-256 为 `5CE879D7E9449C1ED2DE72380CE27F0623BABD762EEFF41C50E5F1CA4C6193D1`；便携 ZIP SHA-256 为 `F71DE085908E4E7603846682ECC44E3859BBE273D0234B4C23CF3A0D695C1A26`。

## 2026-09-17 22:01:44 +08:00 — 修复扩容修改器载入ROM配置越界

- 分支：`main`
- 基线提交：`e3766dcaae0634976a8a363c79e28156e6f98761`
- 改了什么：
  - 修复正式扩容修改器选择部分 ROM 后出现“数组成员引用下标超出定义范围”的载入错误。
  - 更新便携目录、ZIP、使用说明、交付清单和用户 `2.nes` 的 A/B 回归证据。
  - 忽略界面测试产生的自动备份 ROM 和本地会话附件，避免把运行时输入误纳入版本库。
- 怎么改的：
  - 确认旧核心按核心 EXE 所在目录读取配置表，不能只依赖外层工作目录。
  - 构建时将根目录15个 INI/DAT 配置逐字节镜像到 `内部文件/默认配置文件/`，不修改 ROM、核心 EXE 或 CHR 地址补丁。
  - 新增测试，校验内外配置完全一致且便携 ZIP 包含核心旁的码表。
- 影响范围：
  - `tools/build_modifier_skip_launcher.py`
  - `tests/test_modifier_skip_launcher.py`
  - `dist/tools/新DC扩容专用修改器/`
  - `dist/tools/新DC扩容专用修改器.zip`
  - `docs/旧修改器扩容CHR兼容补丁.md`
  - `evidence/2026-09-17-修改器内部配置回归/`
  - `.gitignore`、`dist/MANIFEST.md`
- 验证：
  - 缺少内部配置时，同一 `2.nes` 稳定复现错误代码1及参数 `67859708, 3091`；补齐配置后直接核心和最终外层入口均正常载入。
  - 用户 `2.nes` 测试前后 SHA-256 均为 `64EECD701512448E036730D3C2613F1667CFDAD81CC928A4C82CF6FB7BA24B94`，只读验证未覆盖输入。
  - `python -m unittest tests.test_modifier_skip_launcher -v`：8项全部通过。
  - 外层与内部各15个配置文件按文件名和 SHA-256 比较完全一致；ZIP 不含 `.nes`、`.cdl`、`.deb` 或 `.pdb`。
  - 最终入口 SHA-256 为 `58440C086B3C79B45C14EC18850072E15F2045E9D091B11B2BA50A268584DB9B`；便携 ZIP SHA-256 为 `4D659CD1EC8A4B7F65403A859C343BB3F88EC0E48103B0E06A1855D5339D4BCB`。

## 2026-09-18 00:01:03 +08:00 — 修复新曲与武器音效同时触发的战斗花屏

- 分支：`main`
- 基线提交：`7bec0c601fd2592d5bf6acf5bb0b2871278c307d`
- 改了什么：
  - 修复琉妮使用新曲 `$9A` 时，武器音效 `$18` 在特定 A 键相位触发战斗 CHR 花屏的问题。
  - 同时处理两个独立隐患：重定位驱动占用游戏 `$0028/$0029`，以及 NSF 遗留 `$5FF8` 写入触碰 Mapper 194。
  - 对用户提供的上下两份已修改扩容 ROM 分别生成保留旧区与 CHR 的修复版，并更新正式未绑定构建。
- 怎么改的：
  - 构建时把56个增量音效流转换为完整状态快照；运行时只重放当前快照，单帧成本不随音效年龄增长。
  - 将驱动逻辑 `$0294/$0295` 映射至 `$0468/$0469`，逻辑 `$0296/$0297` 映射至 `$002A/$002B`；桥接状态缩减至 `$046A/$046B`。
  - 将两条 `STA $5FF8,X` 等长改写为 `STA $48F8,X`，并保留交还原引擎前的 FCEUX `$5000=$27` 清锁。
  - 新增只重建 `$60` 桥接槽和 `$64/$74-$77` 音频运行时 Bank 的修复工具，拒绝改动旧地址文件体、15曲数据和活动 CHR。
- 影响范围：
  - `src/asm/dc_refined15_two_engine_bridge.asm`
  - `src/asm/dc_refined15_native_wrapper.asm`
  - `src/lua/dc_refined15_mesen_smoke.lua`
  - `tools/build_refined15_roms.py`
  - `tools/repair_refined15_expanded_roms.py`
  - `tests/test_refined15_build.py`
  - `docs/扩容15曲实现与命令表.md`
  - `docs/琉妮攻击花屏根因与修复.md`
  - `evidence/2026-09-17-琉妮攻击花屏修复/`
  - `dist/roms/`、`dist/MANIFEST.md`
- 验证：
  - FCEUX 2.6.6 上版历史故障延迟 `0,12,27,47,48,57,67,77,78,83,117,118,119` 全部通过；13张关键帧哈希一致，桥接峰值不超过3905周期。
  - FCEUX 上版 `$00-$37` 共56个音效全部结束；15首曲目与 `$18/$19/$1A` 的45组高风险组合全部通过，组合矩阵峰值不超过4530周期、150次 Mapper 写入。
  - FCEUX 上下版15曲矩阵均为 `pass=true tracks=15 failures=0`。
  - Mesen 0.9.9 上下版完整矩阵退出码均为0。
  - `python -m unittest discover -s tests -v`：27项全部通过。
  - 下版没有与其匹配的 Continue 战斗存档，因此未声称执行与上版完全相同的琉妮攻击场景路线。
  - 用户上版修复输出 SHA-256：`B297F83C7EF0E908F7C3B74F36FC8F86B615F78224735F6E92AB673B9CE989B5`；用户下版修复输出：`0E2F64456B2C0AAC97E3968C05BB75A1715108EB9A2DBFB112E782782D29BAAA`。

## 2026-09-18 09:15:00 +08:00 — 修复修改器核心使用后校验失败

- 分支：`main`
- 基线提交：`68b847e877967b666799251475254ba6aea59207`
- 改了什么：
  - 解决修改器完整解压后初次可用、使用一段时间后因工作核心 SHA-256 变化而无法启动的问题。
  - 新增已验证 gzip 核心恢复源，工作核心异常时自动生成干净运行副本。
- 怎么改的：
  - 继续强制校验可执行核心必须为 `4C7F2980CC780253050174C7A6E00A506C7D1EA29E74B90128BDA9ABD9335947`，不将用户截图中的未知 `C890…6BF1` 变体加入白名单。
  - 异常原核心保持不变；只从经二次哈希校验的压缩备份恢复 `修改器核心_自动恢复.exe`。
  - 恢复副本也变化时，下次启动会再次原子替换它。
- 影响范围：
  - `src/modifier_launcher/`
  - `tools/build_modifier_skip_launcher.py`
  - `tests/test_modifier_skip_launcher.py`
  - `dist/tools/新DC扩容专用修改器/`
  - `dist/tools/新DC扩容专用修改器.zip`
  - `docs/旧修改器扩容CHR兼容补丁.md`
  - `evidence/2026-09-18-修改器核心自动恢复/`
- 验证：
  - 隔离测试中故意用错误 EXE 覆盖工作核心；启动后错误文件保持不变，自动恢复核心哈希为 `4C7F…5947`。
  - Windows 实际运行确认恢复核心进入 `SRW2扩容版修改器V1.0`。
  - 恢复副本二次改写后再恢复测试通过；全套自动化测试 29 项通过。

## 2026-09-19 08:58:22 +08:00 — 新增扩容ROM加密解密工具

- 分支：`main`
- 基线提交：`7c65846ff07643085034c57415f486caabf7343d`
- 改了什么：
  - 新增旧 DC 固定偏移“加密/解密”的可审计实现、Tkinter 界面、源码启动器和免 Python 单文件 EXE。
  - 交付 `新DC上_扩容15曲_音频时序修复_已加密.nes`，并更新交付清单、使用说明与验证证据。
  - 新增 MesenCE/FCEUX 冷启动冒烟脚本和加密核心单元测试。
- 怎么改的：
  - 严格校验 iNES 1.0、Mapper 194、Trainer、PRG/CHR 尺寸和关键字节状态，拒绝重复转换或不一致 ROM。
  - 原版仅改 5 字节；1 MiB PRG 扩容版同步 Bank `$3F/$7F` 的 `$07F4C8/$0FF4C8`，仅改 6 字节。
  - 禁止覆盖源 ROM，另存时使用同目录临时文件和原子替换，完成后重读校验状态、偏移和 SHA-256。
  - 用锁定版本的 PyInstaller 及 Windows 打包依赖生成独立 EXE。
- 影响范围：
  - `src/dc_cipher/`
  - `src/lua/dc_cipher_*_boot_smoke.lua`
  - `tools/build_expanded_dc_cipher_exe.py`
  - `tools/dc_cipher_packaging_requirements.txt`
  - `tools/启动扩容ROM加密解密工具.cmd`
  - `tests/test_expanded_dc_cipher.py`
  - `docs/扩容ROM加密解密工具使用说明.md`
  - `evidence/2026-09-19-扩容加密解密工具/`
  - `dist/roms/`、`dist/tools/`、`dist/MANIFEST.md`
- 验证：
  - `python -m unittest discover -s tests -v`：37 项全部通过。
  - 独立 EXE 对目标 ROM 执行加密→解密往返：仅改动 `$4012/$4013/$401E/$401F/$7F4C8/$FF4C8`，往返后与原 ROM 逐字节相同。
  - MesenCE 2.2.1 加密 ROM 持续运行 600 帧，退出码 0。
  - FCEUX 2.6.6 加密 ROM 持续运行 1800 帧，`pass=true`，1799 帧 PC 有效。
  - 原 ROM SHA-256 保持 `B297F83C7EF0E908F7C3B74F36FC8F86B615F78224735F6E92AB673B9CE989B5`；加密 ROM 为 `644951815A3847F888E90885285DA6823C21027AF50BC3F0E94F997B05246929`。

## 2026-09-19 10:38:58 +08:00 — 修复修改器主界面等待超时

- 分支：`main`
- 基线提交：`ce0f6e34a165c2713f1415e29d604dd54015d21c`
- 改了什么：
  - 修复外壳隐藏旧启动页后，单次“进入修改器”消息过早被旧 VB 初始化吞掉，最终报“等待修改器主界面超时”的问题。
  - 修复启动异常后可能留下无窗口内部核心进程的问题。
  - 更新便携交付目录、ZIP、清单、使用说明和运行证据；不修改任何 ROM、扩容地址或内部核心字节。
- 怎么改的：
  - 保留三秒初始化时序，按钮启用后每1.5秒交替发送 `BM_CLICK` 与 `WM_COMMAND/BN_CLICKED`，以主窗口实际出现作为成功条件。
  - 主窗口等待上限由25秒提高到60秒；异常或超时时先恢复隐藏启动页，再终止本次外层启动的核心。
  - 增加源码门禁，防止重新引入只点击一次、不清理残留核心的旧逻辑。
- 影响范围：
  - `src/modifier_launcher/`
  - `tests/test_modifier_skip_launcher.py`
  - `dist/tools/新DC扩容专用修改器/`
  - `dist/tools/新DC扩容专用修改器.zip`
  - `docs/旧修改器扩容CHR兼容补丁.md`
  - `evidence/2026-09-19-修改器启动重试/`
  - 相关入口文档与 `dist/MANIFEST.md`
- 验证：
  - `python -m unittest discover -s tests -v`：38项全部通过。
  - Windows 真实冷启动在约7.6秒内进入 `SRW2扩容版修改器V1.0`；控件树类名为 `WTWindow`，验收退出码0，结束后没有残留修改器进程。
  - ZIP 共34个文件，不含 `.nes`、`.cdl`、`.deb` 或 `.pdb`；ZIP 内入口和说明与交付目录逐字节一致。
  - 内部核心继续锁定 SHA-256 `4C7F2980CC780253050174C7A6E00A506C7D1EA29E74B90128BDA9ABD9335947`。
  - 新入口 SHA-256：`885B053BB4B1B922BD340A7D0034E40825C36B7C19A5328C32A660E105D9095C`；便携 ZIP：`3196A3B4CBC123516C5E235151ED2C34469D6461CFB150C31AE78360E770E21F`。

## 2026-09-20 08:35:38 +08:00 — 接入扩展曲目并完善精修驱动转换流程

- 分支：`main`
- 基线提交：`ecb001a93bc668cbd8c5aff032f6b969b3583263`
- 改了什么：
  - 将 `LAST IMPRESSION` 以命令 `$A6`、逻辑编号 `$23` 和数据 Bank `$78` 接入下版扩容 ROM。
  - 将五首《三目童子》重建为精修公共驱动 NSF；在上下版 ROM 中以 `Stage 5-2` 替换 `$A0/$6F` 的 `JUST COMMUNICATION`，并通过 `$A7-$AA` 加入其余四首。
  - 将《忍者龙剑传 - 鲜烈之龙（4-2）》转换为精修公共驱动 NSF，保留两份 DPCM 采样；同时交付琉妮 ROM 同驱动柔和数据版 NSF。
  - 新增可编辑 FamiStudio 工程、转换与 ROM 接入工具、可复用 `nsf-refined-common-driver` skill、运行时回归脚本、自动化测试、说明文档和验证证据。
- 怎么改的：
  - 通过 6502/APU 轨迹和完整状态重复探测真实循环，以 FamiStudio 重建逐帧音乐数据，再绑定锁定的 `$F000/$F100/$F160` 公共驱动；鲜烈之龙另校验非对齐 NSF Bank、DPCM 字节和触发时刻。
  - 扩展 ROM 保持原 PRG、CHR、既有精修曲、音效及固定 Bank 不变；用独立上下版桥接器、数据映射器和调度器分配 `$6F/$78-$7D`，Boss Fight 的第三数据页及驱动放入 `$7B`。
  - 构建器锁定输入哈希和允许修改范围，不原地覆盖输入；交付清单记录所有新 ROM/NSF 的大小、SHA-256、命令和 Bank。
- 影响范围：
  - `assets/music/`
  - `src/asm/`、`src/lua/`
  - `tools/add_last_impression_to_expanded_rom.py`
  - `tools/add_mitsume5_replace_just_to_expanded_roms.py`
  - `tools/convert_mitsume_to_refined_driver.py`
  - `tools/convert_ninja_gaiden_to_refined_driver.py`
  - `tools/rebuild_flapper_soft_with_rom_driver.py`
  - `tools/codex-skills/nsf-refined-common-driver/`
  - `tests/`、`docs/`、`evidence/`
  - `dist/music/`、`dist/roms/`、`dist/MANIFEST.md`
- 验证：
  - `python -m unittest discover -s tests -v`：54 项全部通过。
  - 三目童子上下版 Mesen 五曲冒烟、下版 `$A6/$78` 单曲复测通过；FCEUX 上版 19 曲、下版 20 曲矩阵均为 `pass=true` 且零失败。
  - 五首三目童子和鲜烈之龙的精修驱动回绑逐帧听觉状态零差异；鲜烈之龙两份 DPCM 内容一致、197 次触发无错位。
  - `git diff --check` 通过，仅显示工作区 LF/CRLF 转换提示。
